{
  "name": "subagents",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "description": "Automatically delegates matching Pi tasks to Markdown-configured subagents.",
  "main": "index.ts",
  "keywords": [
    "pi-package"
  ],
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  },
  "scripts": {
    "format": "bunx prettier --write .",
    "check": "bun run check:syntax && bun run eval:conversation-context",
    "check:syntax": "node --experimental-strip-types --check index.ts agents.ts runtime/*.ts ui/*.ts evals/orchestration/run.ts evals/orchestration/cases.ts evals/conversation-context/run.ts",
    "eval": "bun run eval:conversation-context && bun run eval:orchestration",
    "eval:conversation-context": "bun evals/conversation-context/run.ts",
    "eval:orchestration": "bun evals/orchestration/run.ts",
    "eval:orchestration:strict": "bun evals/orchestration/run.ts --strict-soft",
    "eval:list": "bun evals/orchestration/run.ts --list"
  },
  "dependencies": {
    "@sinclair/typebox": "^0.34.49"
  },
  "peerDependencies": {
    "@mariozechner/pi-agent-core": "*",
    "@mariozechner/pi-coding-agent": "*",
    "@mariozechner/pi-tui": "*"
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  }
}
